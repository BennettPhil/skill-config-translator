# config-translator

Converts between config formats (JSON, YAML, TOML, INI, dotenv) with lossy conversion warnings.

## Quick Start

```bash
echo '{"host": "localhost", "port": 8080}' | ./scripts/run.sh --from json --to yaml
```

## Full Documentation

See [SKILL.md](SKILL.md) for usage examples, smoke tests, and options reference.
